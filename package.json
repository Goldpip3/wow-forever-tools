{
  "name": "wow-forever-tools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run addon && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "import": "node scripts/import-talentsforever.mjs && node scripts/scrub-sources.mjs",
    "assets": "node scripts/fetch-assets.mjs",
    "addon": "node scripts/pack-addon.mjs",
    "typecheck": "tsc --noEmit",
    "e2e": "playwright test",
    "import:items": "node scripts/import-items.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.5.1",
    "fengari": "^0.1.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
